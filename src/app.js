const Vue = require('vue')
const store = require('./store')
require('./render')
require('./interact')
// store.$watch('current',(value)=>{
//     value == 'zone'?require('./render'):require('./table')
// })
